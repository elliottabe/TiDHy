user: eabe

# set the directory where the output files get saved
base_dir: /data/users/${paths.user}/hypernets/${dataset.name}/DPC_${dataset.name}/${train.version}
save_dir: ${paths.base_dir}/${hydra:job.override_dirname}
log_dir:  ${paths.save_dir}/
tb_dir:   ${paths.save_dir}/tb/
ckpt_dir: ${paths.save_dir}/ckpt/
fig_dir:  ${paths.save_dir}/figures/
data_dir: /data/users/${paths.user}/hypernets/${dataset.name}/datasets/